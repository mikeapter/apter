# Copy this file to .env and fill in real values
#
# IMPORTANT:
# This repo is configured as a SIGNAL-ONLY TRADING TOOL (signal generation only).
# Automated execution is intentionally disabled in code.

# ------------------------------------------------------------
# Platform mode (hard safety)
# ------------------------------------------------------------
BOTTRADER_EXECUTION_MODE=TOOL
BOT_EXECUTION_MODE=TOOL
EXECUTION_MODE=TOOL

# ------------------------------------------------------------
# Bot identity / runtime
# ------------------------------------------------------------
BOT_ID=opening

# ✅ API/runtime manager uses these:
BOTTRADER_DEFAULT_SCRIPT=scripts/run_opening_tool.py
BOTTRADER_OPENING_SCRIPT=scripts/run_opening_tool.py

# Backward-compat alias
BOT_DEFAULT_SCRIPT=scripts/run_opening_tool.py

# ------------------------------------------------------------
# Paths (relative to repo root)
# ------------------------------------------------------------
RUNTIME_DIR=Platform/runtime

# ------------------------------------------------------------
# Local ports (dev)
# ------------------------------------------------------------
API_PORT=8000
WEB_PORT=3000

# ------------------------------------------------------------
# Public domain / canonical URLs
# ------------------------------------------------------------
PRIMARY_DOMAIN=apterfinancial.com
PUBLIC_BASE_URL=http://localhost:3000
CANONICAL_URL=https://apterfinancial.com

# ------------------------------------------------------------
# Local dev API key policy (placeholder)
# ------------------------------------------------------------
LOCAL_DEV_API_KEY=

# ------------------------------------------------------------
# AI Assistant configuration
# ------------------------------------------------------------
AI_PROVIDER=openai_compatible
AI_API_KEY=
AI_MODEL=gpt-4o-mini
AI_BASE_URL=https://api.openai.com/v1
AI_ENABLE_STREAMING=true
AI_CACHE_TTL_SECONDS=300
AI_TIMEOUT_SECONDS=60
