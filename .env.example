# ----------------------------------------
# BotTrader - Environment Example (SAFE)
# Copy to .env and fill in real values
# ----------------------------------------

# ========= Runner settings =========
# This is the script the Platform/control-plane will call.
# Keep it pointing to your existing runner unless you decide otherwise.
BOT_RUNNER=scripts/run_opening.py

# Optional extra args (leave blank if not needed)
BOT_RUNNER_ARGS=--config Config/opening_playbook.yaml

# Which python to use when launching the runner
PYTHON_EXE=python

# ========= Control Plane (FastAPI) =========
API_HOST=127.0.0.1
API_PORT=8000
API_LOG_LEVEL=info

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000

# ========= Web (Next.js) =========
WEB_HOST=localhost
WEB_PORT=3000

# ========= Security (PLACEHOLDERS) =========
# Replace in .env with real secrets (do NOT commit .env)
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING
ADMIN_TOKEN=CHANGE_ME_ADMIN_TOKEN

# ========= Bot runtime mode =========
# Keep PAPER for safety. (LIVE should come much later.)
BOT_MODE=PAPER

# Paper broker seed (optional)
PAPER_SEED=42
