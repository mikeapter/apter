# Opening tool playbook (signals-only)

universe:
  symbols:
    - AAPL
    - MSFT
    - SPY

strategy:
  id: opening_v1
  default_qty: 1

output:
  signals_dir: Platform/runtime/signals
  file_prefix: signal

explain:
  include_trace: true
