# Local dev environment (Option A)
# Use Next.js route handlers on the same origin (localhost:3000).

API_BASE_URL=http://127.0.0.1:3000

# IMPORTANT:
# Leave NEXT_PUBLIC_API_BASE_URL unset so the browser calls /api/... on localhost:3000
# (If you set this to :8000, the UI will try to fetch FastAPI directly.)
# NEXT_PUBLIC_API_BASE_URL=
